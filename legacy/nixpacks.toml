[phases.setup]
nixPkgs = ["python39", "pip"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements_production.txt"
]

[phases.build]
cmds = []

[start]
cmd = "gunicorn app_production:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
