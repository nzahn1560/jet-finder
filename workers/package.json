{
    "name": "jetfinder-workers",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "migrate": "wrangler d1 migrations apply jetfinder-db",
        "migrate:local": "wrangler d1 migrations apply jetfinder-db --local",
        "migrate:create": "wrangler d1 migrations create"
    },
    "dependencies": {},
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20241127.0",
        "typescript": "^5.6.3",
        "wrangler": "^3.72.0"
    }
}